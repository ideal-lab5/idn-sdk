apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: idn-collator
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/upstream-hash-by: "$binary_remote_addr"
    nginx.ingress.kubernetes.io/websocket-services: "mainnet-us-idn-collator"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - idn-us-01.idealabs.network
      secretName: idn-collator-tls
  rules:
    - host: idn-us-01.idealabs.network
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mainnet-us-idn-collator
                port:
                  number: 9944

[package]
name = "sc-consensus-randomness-beacon"
version = "1.0.0"
authors.workspace = true
edition.workspace = true
license = "MIT-0"
repository.workspace = true
description = "Randomness-Beacon Listener consensus client"
homepage = "https://idealabs.network"

[lints]
workspace = true

[dependencies]
# libp2p (uses same version as substrate)
libp2p = { version = "0.52.4", features = ["gossipsub", "noise", "tcp", "tokio", "yamux"] }
prost = "0.13.4"
prost-types = "0.13.4"
sp-consensus-randomness-beacon = { path = "../../../primitives/consensus/randomness-beacon" }
log = { workspace = true }
futures = "0.3"
